# Source

This folder contains the original Vite/React prototype source.

The current live static page is driven by:

```text
index.html
styles.css
script.js
```

Only migrate behavior into `src/` when the project is intentionally converted back to a React app.

